# 更新记录

## 0.3.0

- 工作模式内按 `/` 临时唤起命令框；执行命令或 Esc 返回后继续锁定文字输入，支持 `/work off` 退出模式。
- `/auto 10` 按秒自动翻动，沿用 `/step` 行数；`/auto off` 停止，`/auto` 查看状态。
- 输入命令、使用菜单、隐藏正文或窗口过小时暂停计时；返回后等待完整间隔，到书末、换书和退出时停止。
- 忽略工作模式下直接粘贴的括号粘贴内容，避免其中的斜杠意外打开命令框。

## 0.2.0

- `/width auto` 让正文跟随窗口宽度；固定宽度范围扩大为 36–500 列，保留旧版设置。
- `/reflow on` 合并中文正文的硬换行，保留章节、空行、缩进与短诗行；不修改文件或阅读位置。
- `/work` 或 F2 开启乱打字模式：忽略文字、空格、回车和普通快捷键，方向键继续按 `/step` 翻页，F2 恢复输入。
- 新增 `--width`、`--reflow`、`--work` 启动参数。
- 识别章回编号中的“○”，兼容更多公版小说的目录。

## 0.1.2

首个公开发布到 npm 的版本。

- 本地 TXT / 纯文本 Markdown 阅读，支持常见中文编码。
- 深色、浅色、终端主题，以及代码助手风格。
- 章节目录、全文搜索、书签和自动续读。
- `/step` 调整每次翻动行数，自动保存设置。
- 修复 ↑ / ↓ 不遵循 `/step` 设置的问题；j / k 保留逐行移动。
- 清理输入文本中的终端控制序列，预览模式不写入阅读记录。
